# python-File-Transfer-App
This Python script creates a basic HTTP server using http.server and socketserver. It sets port 8000, initializes a simple request handler, and starts a server that runs indefinitely. When executed, it hosts files from the current directory and prints a message indicating the server
